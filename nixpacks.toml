[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = [
  "python3 -m pip install -r requirements.txt",
  "python3 -m playwright install chromium --with-deps"
]

[start]
cmd = "python main.py"
